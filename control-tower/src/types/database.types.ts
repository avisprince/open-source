export enum SUPPORTED_DATABASES {
  MYSQL = 'mysql',
  POSTGRES = 'postgres',
  MONGO = 'mongodb',
}

export enum DB_PORTS {
  MYSQL = 3306,
  POSTGRES = 5432,
  MONGO = 27017,
}

export enum DB_USERS {
  MYSQL_ADMIN = 'root',
  POSTGRES_USER = 'postgres',
}
