export enum SidebarTabs {
  sandboxes = 'sandboxes',
  templates = 'templates',
  tests = 'tests',
  organizationProfile = 'organization',
  team = 'team',
  settings = 'settings',
  connections = 'connections',
  dbFiles = 'dbFiles',
}
